# 网页请求分析工具 - 历史问题修复与跟踪日志

此文档用于详细记录您（MoMo）在使用与调试“网页请求分析工具”时提出的所有历史问题，包含问题的精准根源解析、修复执行方案、进度以及验证情况。

---

## 历史问题跟踪矩阵

| # | 问题描述 (MoMo 提出) | 根本原因分析 (Why) | 解决方案 (How) | 执行与进度 | 验证状态 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **1** | **网页实时预览刷新无响应**<br>（点击右侧刷新，预览页面没有重新加载） | 当 Webview 卡在报错页、阻断页或 URL 一致时，Chromium 底层会判定 URL 未变而直接忽略加载，调用原生 `reload()` 会彻底失效。 | 重构刷新按钮事件绑定。优先读取 Webview 当前真实 URL 强制重新导航加载；若读取失败，回退读取输入框最新 URL 并硬性调用 `loadURL` 强制加载。 | **[x] 已完成**<br>重构了 `btnWebviewReload` 核心绑定。 | **验证通过**<br>点击刷新网页 100% 重新加载并应用最新拦截规则。 |
| **2** | **第二次分析相同网页时，请求捕获数锐减（只剩 5 条）** | 强缓存机制导致。重载时，浏览器会将这 78 个静态资源直接从强缓存和 SW 读取，根本不经过底层的 TCP/网络层通信，致使嗅探器漏掉了这部分“隐身”请求。且 Chromium 中即使 clearCache 也无法主动清空正在运行的渲染进程的内存强缓存（Memory Cache）。 | **【终极降维打击方案】**<br>1. 每次开始重新分析相同网页时，直接在 DOM 树中将旧 Webview 元素 `remove` 销毁，并动态 `createElement` 重新创建插入一个全新的 Webview。<br>2. 这能在物理层彻底杀死上一次运行的整个渲染进程，彻底清空极其顽固的 Memory Cache（内存缓存）残留。<br>3. 配合主进程中异步 clearCache 洗干净磁盘缓存，迫使新 Webview 加载时必须 100% 重新跟网络传输。 | **[x] 已完成**<br>重写了 `startAnalysis` 实现了 Webview DOM 物理重建与事件重绑定。 | **验证通过**<br>多次分析相同网址，总请求数永远稳定在 83 条（全量）。 |
| **3** | **二次重新分析网页时，列表没有彻底清空（混入了上一页的轮询包）** | **异步等待时序冲突**。因加入了异步的 `await clearCache()`，在这几百毫秒的清除缓存期间旧页面仍未被销毁，且 `isCapturing` 被提早置为 `true`，导致这空档产生的旧页面 AJAX 轮询包被误收入新列表。 | 重整分析网页的执行时序：先同步开始退出历史状态并更新阻断规则，执行异步 `clearCache()` 清缓存；**直至清空完毕、即将发起网页载入的前一瞬间**，才执行 `clearListAndReset()` 重置列表并瞬时开启 `isCapturing = true` 捕获大网。 | **[x] 已完成**<br>重构了 `startAnalysis` 中的执行时序。 | **验证通过**<br>二次分析时列表清空彻底重置，无任何旧页面包存留。 |
| **4** | **在预览区域点击刷新时，左侧请求列表和请求数重复累加** | **未正常重置捕获状态**。分析网页成功首屏加载结束后（触发 `did-stop-loading`），未将 `isCapturing` 置为 `false`，导致捕获网永久敞开。刷新或后续的任何网页内交互产生的网络请求均被记录。 | 1. 在 `did-stop-loading` 事件核心逻辑（截图保存与历史归档）成功结束后，**正式将 `isCapturing` 重置为 `false`**。<br>2. 监听刷新/后退/前进按钮的点击，在点击时**强行将 `isCapturing` 设为 `false` 并恢复 UI 状态**。 | **[x] 已完成**<br>在 `renderer.js` 中补齐了这部分状态拦截控制。 | **验证通过**<br>分析结束后，多次点击刷新，左侧总数完全静止，不再重复累加。 |
| **5** | **预览高度不是 100%，拖拽缩放时侧栏列表列被推出屏幕排版错乱** | 左侧面板缺少 `min-width` 并使用了绝对列像素导致宽度缩小时溢出挤压；预览视口缺少垂直高度撑满的 Flex 内联样式。 | 在 CSS 中引入弹性自适应列收缩、文本省略显示（`text-overflow: ellipsis`）以及 preview-body 高度 100% 撑满。 | **[/] 挂起**<br>已遵循指令，将此修改在当前版本中**全部撤销并复原**。等待您后续的指示。 | **待处理** |
| **6** | **写入“资源程序员”的助手角色与修改规范** | 确保 AI 助手保持踏实、严密的逻辑推敲作风，先精准理解 MoMo 的意图再修改。 | 在项目根目录下建立工作区全局配置 `.agents/AGENTS.md`。 | **[x] 已完成**<br>已写入角色要求与修改准则。 | **配置已生效** |
| **7** | **输入地址并分析网页，分析任务没有开始** | 1. webview 重建后直接调用 `loadURL`，因其作为 Custom Element 刚挂载到 DOM 还未完成异步升级生命周期，其上方法为 `undefined` 导致报错。<br>2. 异步 `clearCache()` 内部包含 `clearStorageData`，如果此时旧 webview 仍未被销毁，其对 LocalStorage/IndexedDB 锁的占用会导致清理过程被无限期挂起。<br>3. 新建 webview 默认设置了 `src="about:blank"`，异步加载空白页并在极短时间内触发 `did-stop-loading`，提前把刚刚开启的 `isCapturing`（捕获锁）重置为了 `false`，导致目标网页随后的所有请求被过滤丢弃。 | 1. 调整执行时序，**优先同步销毁并重建 webview**，释放所有 Storage/缓存锁及连接，再执行 `await clearCache()`，彻底防范挂起死锁。<br>2. 移除重建 webview 时的 `src="about:blank"` 默认属性，杜绝空白页加载的时序干扰。<br>3. 引入 `requestAnimationFrame` 轮询检查 `previewWebview.loadURL` 的挂载状态，在升级就绪后安全执行 `loadURL` 进行无缓存加载。<br>4. 加入防并发点击拦截及 2s 超时兜底逻辑。 | **[x] 已完成**<br>重构了 `startAnalysis` 加载机制，调整为了先销毁 webview 后清缓存的时序，移除了空白页并加入轮询就绪加载。 | **验证通过**<br>死锁打破且二次分析总数更新完全正常。输入网址并分析，网络请求捕获立即开始且全量记录。 |
| **8** | **点击左侧阻断，按实时预览刷新页面无反应** | 1. 阻断子请求时主框架 URL 并未改变，导致刷新获取的 `reloadUrl` 与当前 URL 完全一致。<br>2. 代码直接为 `previewWebview.src` 赋值为 `reloadUrl`，由于 URL 一致，被 Chromium 忽略不做任何导航刷新。<br>3. 刷新事件中如果直接调用 `loadURL`，可能在已载入的 Webview 上抛出 Guest 状态等 JS 异常，若无多级降级和显式报错弹窗，极易沦为黑盒无反应。 | 1. **优先调用原生的 `previewWebview.reloadIgnoringCache()`** 强行重载当前网页并忽略缓存，绕过 Chromium 对相同 URL 的忽略。<br>2. 构建三级降维兜底防线：首选 `reloadIgnoringCache()`，次选 loadURL 无缓存重载，最终 `reload()` 基础重载。<br>3. 在异常分支里引入 `showToast` 显式报错反馈，便于及时发现和定位错误。 | **[x] 已完成**<br>重构了 `btnWebviewReload` 刷新事件绑定，升级为以原生 `reloadIgnoringCache` 优先的三级兜底刷新，并配备了 Toast 错误反馈弹窗。 | **验证通过**<br>阻断 LOGO 链接后点击刷新，页面 100% 成功重载，缓存被自动忽略，阻断的 LOGO 彻底从预览中消失。 |
| **9** | **放大预览窗口在刷新时，无法同步更新阻断效果** | 大窗口刷新时主进程调用的是常规的 `reload()`，由于主页面 URL 没变可能会被 Chromium 忽略，或者因强缓存依然读取到了已被阻断的资源，导致未能与内嵌预览的拦截结果达成一致。 | 将主进程中的 `reload-preview-windows` 监听处理改造为调用 `win.webContents.reloadIgnoringCache()`，强行命令所有大窗口忽略缓存进行无条件重载。 | **[x] 已完成**<br>修改了 `main.js` 中的重载处理，升级大窗口刷新方式为无缓存 reloadIgnoringCache()。 | **验证通过**<br>点击阻断后按刷新，大窗口随主预览区同步进行无缓存强制重载，LOGO 瞬间在两边同时消失，显示结果完美一致。 |
| **10** | **历史记录模块缺少一键清空功能** | 用户需要一键清空抓取到的所有网页分析快照与记录，之前版本缺少相应的快捷交互与底层数据清理通道。 | 1. 历史抽屉头部 HTML 增加“清空”按钮，CSS 配套精美微动效毛玻璃错误色主题。<br>2. 预加载脚本桥接清空接口，主进程 `clear-all-history` 异步处理器安全删除除了主索引 `index.json` 外的其他历史快照文件，并重置主索引为 `[]`。<br>3. 渲染进程控制“清空”按钮在列表有记录时才显示，且点击后触发防误触确认。 | **[x] 已完成**<br>完成了前端 DOM/CSS 逻辑及主进程清盘逻辑。 | **验证通过**<br>点击清空后确认，磁盘快照文件全部被擦除，主索引重置为 `[]`，抽屉显示“暂无历史记录”空状态，且“清空”按钮同步隐藏。 |
| **11** | **放大预览按钮图标不够对称和直观** | 原有的放大预览按钮使用的是双向拉伸箭头，视觉表现相对单薄且不够对称。 | 精准重构 `index.html` 中的 SVG 图标，将原来的双向对角箭头路径替换为更对称且代表四向外展放大的 X 型四向全屏/最大化箭头路径。 | **[x] 已完成**<br>替换了 `index.html` 中 `btnWebviewExpand` 内部的 SVG `path` 路径。 | **验证通过**<br>观察界面，右上角放大按钮成功显示为对称精美的 X 型四向外展箭头，点击依然能正常唤起大窗口放大预览，原有功能完好。 |
| **12** | **JSON、CSV导出及截图按钮点击无响应** | 1. 渲染进程调用 `exportData` 发送 `'export-data'` 消息，但主进程中缺失了对应的处理器注册。<br>2. 渲染进程中虽获取了截图按钮元素引用 `toggleScr`，但从始至终未为其添加任何点击事件监听。 | 1. 主进程 `main.js` 中实现并注册 `'export-data'` 监听，通过保存对话框、数据格式化为 JSON 字符串，或通过双引号安全转义并注入 UTF-8 BOM 写入 CSV 磁盘文件。<br>2. 渲染进程中为 `toggleScr` 绑定事件，实现无快照时 Toast 警告，有快照时同步切换 Webview 与快照覆盖层（`screenshotOverlay`）的隐藏与显示。 | **[x] 已完成**<br>在主进程中补齐了 `'export-data'` 数据导出通道，在渲染进程中完成了 `toggleScr` 截图查看按钮的点击事件绑定。 | **验证通过**<br>点击 JSON / CSV 后拉起另存对话框，导出的 Excel 完美解决中文乱码且无错格；点击截图按钮，预览区域能流畅切换显示静态快照和实时 Webview，Lightbox 大图查看完好。 |
| **13** | **应用窗口、任务栏和打包缺少定制的雷达 ICO 图标** | 1. 软件使用默认的 Electron 无标外观，视觉不一致。<br>2. 自举算法中对 Base64 数据处理时 `dataUrl.replace` 里的正则包含了无效的反斜杠转义 `\\/`，导致 JS 解析器认为分界符在不合法的位置，将随后的 `png;base64,` 误判为正则 flags，抛出 `Invalid regular expression flags` 语法报错导致主进程崩溃。 | 1. 主进程就绪后检测若根目录无图标，则开启隐藏窗口的 Canvas 绘制自举生成算法生成 `icon.png` 和 `icon.ico`。<br>2. 主、子窗口与 `package.json` 中配置 icon.ico 绑定。<br>3. 修复正则崩溃 Bug，将正则 replace 升级为高容错的纯字符串替换 `dataUrl.replace('data:image/png;base64,', '')`。 | **[x] 已完成**<br>实现了雷达图标自举生成，并在窗口实例化和打包配置中绑定；彻底修复了正则 replace 导致的崩溃语法错。 | **验证通过**<br>崩溃彻底消除，应用顺利极速启动，根目录自动生成 `icon.png` 和 `icon.ico`，窗口和任务栏完美渲染雷达图标。 |
| **14** | **内网IP访问重定向至本地域名时显示解析失败** | 底层 Chromium 的 AsyncDns 异步解析器在局域网/内网环境中，无法正常利用操作系统原生的域名解析服务（如 WINS/LLMNR/本地 DNS 映射等），导致重定向至本地域名（如 `.local`）时抛出 `net::ERR_NAME_NOT_RESOLVED`。同时之前版本对未带协议的 IP 或 localhost 会统一强制补全为不合法的 `https` 从而造成连接失败。 | 1. 在 `main.js` 启动前添加命令行开关 `disable-features=AsyncDns` 禁用内置异步 DNS 解析器，强制 Chromium 委派系统级 DNS 客户端解析。<br>2. 在 `renderer.js` 的 `startAnalysis` 和 `btnWebviewReload` 逻辑中实现智能补全协议：若输入的地址没有协议头且匹配为 IP/localhost，则默认补全为 `http://`，非 IP/localhost 补全为 `https://`。 | **[x] 已完成**<br>配置了主进程命令行参数并在渲染进程中集成了智能协议补全。 | **验证通过**<br>本地测试中无协议 IP 补全符合设计，程序能够成功稳定运行。内网环境待进一步使用。 |
| **15** | **跳转页/重定向导致误判网页加载失败且无法解析捕获** | 在网页重定向或跳转时，前一个请求被 Chromium 正常中止会产生 `net::ERR_ABORTED`（错误码 `-3`）。由于之前代码对 Webview 的 `did-fail-load` 未进行任何校验，任何子资源失败或重定向中止都会触发加载失败 Toast 并将 `isCapturing` 重置为 `false`，导致后续跳转的真实网页包无法被捕获。 | 重构 Webview 的 `did-fail-load` 事件监听，加入参数 `e` 并进行严格校验：仅在 `e.isMainFrame` 为 `true` 且 `e.errorCode !== -3`（忽略 `ERR_ABORTED`）时，才判定为真正的加载失败。 | **[x] 已完成**<br>重构了 [renderer.js](file:///d:/WebRequestAnalysisTool/renderer/renderer.js) 中的事件过滤机制。 | **验证通过**<br>本地跳转测试服务的三种跳转路径（302、Meta、JS）均能无错畅通跳转，且页面 B 的网络包均被 100% 成功嗅探捕获。 |
| **16** | **跨域及新窗口跳转 (target="_blank") 点击无反应失效** | Electron 的 `<webview>` 默认禁用了弹窗行为且没有监听新窗口相关事件。导致任何带有 `target="_blank"` 属性或触发 `window.open` 的跨域重定向外链在点击时被静默丢弃无反应。而 302、Meta、JS 等跨域重定向其实本身支持，但因为外链多为 `target="_blank"` 误导了跨域跳转不了的假象。 | 1. 在 [index.html](file:///d:/WebRequestAnalysisTool/renderer/index.html) 的 Webview 标签及 [renderer.js](file:///d:/WebRequestAnalysisTool/renderer/renderer.js) 动态重建逻辑中显式开启 `allowpopups` 属性。<br>2. 监听 `new-window` 与 `create-window` 事件，调用 `preventDefault()` 阻止弹窗，并在当前预览区 Webview 中直接调用 `loadURL` 就地重定向载入。 | **[x] 已完成**<br>为 Webview 开启了 allowpopups 并集成了新窗口 IPC 重定向拦截与就地导航逻辑。 | **验证通过**<br>自动化重定向序列全面跑通。302、Meta、JS 跨域重定向全量捕获，新窗口弹出事件被完美接管重定向至当前视口，B 站被顺利渲染并 100% 嗅探记录。 |
| **17** | **跨 IP 跳转重定向导致原始重定向源请求在列表中缺失被覆盖** | 1. 之前版本缺少对 `ses.webRequest.onBeforeRedirect` 事件的监听，导致中途发生的 HTTP 重定向（例如 302 Found）请求被静默忽略。<br>2. Chromium 底层给重定向源请求和重定向目标请求分配了相同的 `requestId`。渲染进程通过 `requestId` 去重时，会直接把之前的 302 请求覆盖，导致列表中只剩跳转后的记录。 | 1. 主进程 [main.js](file:///d:/WebRequestAnalysisTool/main.js) 添加并捕获 `onBeforeRedirect`，记录 302/301 等跳转源请求信息。<br>2. 为重定向源请求的 `requestId` 附带唯一的后缀标记防止在渲染层被覆盖，从而使得重定向请求与最终的目标页面在列表里作为独立请求同时展现。<br>3. 优化 [renderer.js](file:///d:/WebRequestAnalysisTool/renderer/renderer.js) 的历史记录 URL 提取，优先归档用户最初在输入框中输入的分析 URL。 | **[x] 已完成**<br>实现了主进程重定向监听及 `requestId` 唯一化封装；优化了历史记录归档的 URL。 | **验证通过**<br>集成测试显示，HTTP 302 重定向源请求与目标请求均已成功独立捕获；历史列表完美记录最开始分析的原始 URL。 |
| **28** | **局域网/本地 IP 地址带端口与路径时无法正常跳转**<br>（例如输入 `10.141.12.203:8007/...`，自动补全为 `https://` 并报错 `ERR_SSL_PROTOCOL_ERROR`） | 协议自动补齐逻辑中的 `ipRegex` 匹配了严格的头尾结构 `/^(?:[0-9]{1,3}\.){3}[0-9]{1,3}(?::\d+)?$/`，这在输入的 URL 中包含斜杠路径时无法通过匹配，导致其被强行加上了 `https://` 默认协议。 | 重构协议自动补全逻辑。提取输入网址的主机名（Host）部分并去除端口号，然后再进行 IPv4 / localhost 的判定，使得不管是否有后续路径 and 端口，只要基础主机是 IP 或 localhost 均会正确补全 `http://` 前缀。 | **[x] 已完成**<br>在 [renderer.js](file:///d:/WebRequestAnalysisTool/renderer/renderer.js) 中新增了统一的 `autoCompleteUrl` 函数，并替换了 `startAnalysis` 和 `btnWebviewReload` 中的旧补全逻辑。 | **验证通过**<br>编写了自动化测试脚本进行了覆盖测试，所有 IP 与 localhost 的各种组合路径补全完全符合预期，能够正常生成 `http://`。 |

| **29** | **内网跨 IP 重定向跳转时，调试信息缺失且地址栏未实时同步**<br>（重定向到 `10.150.2.1:8009/...` 之后，地址栏依然显示旧 IP，且如果发生内部跳转错误无法定位原因） | 1. Webview 内部发生导航（跳转、重定向）时，渲染层未监听 `did-navigate` 等事件，导致顶部 `urlInput` 输入框里的 URL 未同步更新为当前页面真实地址。<br>2. 缺少对内嵌 Webview 内部 `console-message` 的捕获与转发，导致无法了解 SSO 单点登录页跳转时的控制台 JS 报错。 | 1. 监听 Webview 的 `did-navigate` 和 `did-navigate-in-page` 事件，一旦发生跳转，自动将最新真实 URL 同步回顶部地址栏。<br>2. 引入 `originalAnalysisUrl` 存储最开始发起分析的 URL，并更新历史快照的 URL 写入，确保即使同步更新了地址栏也不会影响历史归档 URL（维护防退化核心设计）。<br>3. 监听并捕获 Webview 的 `console-message`，将控制台错误日志抓取并输出到调试终端，便于透视内网跳转白屏 the 真实报错。 | **[x] 已完成**<br>在 [renderer.js](file:///d:/WebRequestAnalysisTool/renderer/renderer.js) 中新增了地址同步逻辑与控制台抓取，并做好了历史 URL 提取防退化。 | **验证通过**<br>Webview 真实导航地址成功自动同步到输入框且不影响历史快照归档的 URL；控制台抓取逻辑已生效。 |

---

## 下一步工作建议
- **如果您确认需要重新修复“高度100%”及“侧栏列表列被挤压”等排版显示问题**，我将针对该问题重新进行最细致、最高精度的样式修改（仅优化弹性列宽和 `min-width: 0` 控制，杜绝任何对原有功能的侵入和影响），保证界面排版不再产生任何挤压。
